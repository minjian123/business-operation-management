# 英文简称规范

> 本项目散落英文简称统一口径

[文档首页](../文档首页.md) › 规范 › 英文简称规范　|　[同级：命名规范 →](命名规范.md)

## 1. 目的与适用范围 <a id="purpose"></a>

统一本项目内散落于各类文档（规划、规范、设计、说明）中的英文简称理解口径，防止同一缩写出现多义、
歧义或按通用外义误读。本表为**理解口径**：标识符的*写法规则*（大小写、格式）以[《命名规范》](命名规范.md)为准，
本表只约束**简称的含义**；与《命名规范》冲突时以《命名规范》为准。

## 2. 通用原则 <a id="principle"></a>

- **项目内含义优先**：同一缩写若通用外义与本项目含义不同，一律以本表"项目内含义"列为准（如 open 在本项目指开放接口业务，不是"打开"）。
- **先查后写**：写作或阅读时遇缩写，先查本表；本表未收录的缩写须先登记（见第 9 节）再使用，禁止临时自造。
- **大小写**：通用缩写词全大写（JWT、RBAC、SSO）；模块简称、业务码、动作码、事件域全小写（user、pur、sale、query）；组件名保留官方写法（Element Plus、GitLab CE）。
- **英文来源**：本项目采用美式英语拼写；缩写来源给出全称便于理解，但不要求按全称拼写。

## 3. 项目专用简称 <a id="project"></a>

| 简称 | 全称/来源 | 项目内含义 | 备注 |
| --- | --- | --- | --- |
| BMS | Base Management System | 基础管理系统（本项目名称） | 项目、镜像名 bms-*、Redis key 前缀 bms: 均源于此 |
| MVP | Minimum Viable Product | 最小可用版本：开发计划一至十五阶段完成后的首发版本 | "MVP 模块"= 首发包含的功能模块；"明确不采用"条款亦为 MVP 口径 |
| JIT | Just-In-Time | SSO 首次登录自动建号（JIT 建号） | 见"身份认证（SSO）"模块 |
| SaaS | Software as a Service | 软件即服务：多租户形态（租户独立库） | — |
| IdP | Identity Provider | 身份提供方：外部 OIDC/CAS 提供方；本平台兼作 IdP | 与业务码 idp（IdP 配置）区分 |
| tenant | 租户 | 多租户模型中的独立租户；租户库 bms_tenant_{code}、租户路由、租户配额 | 同时为业务码 tenant（租户管理） |
| graphify | — | 知识图谱工具（CLI 命令 graphify），辅助代码理解与架构分析 | 输出 graphify-out/；文档《graphify 部署使用说明》 |
| AGENTS.md | — | AI 助手项目约定文件（仓库根目录，AI 协作约束） | — |

## 4. 模块与域简称 <a id="module"></a>

模块简称同时用于：表前缀（{简称}_）、业务码（sys_business）、事件域（{简称}.{动作}）、后端目录（app/api/{简称}/）与前端目录（views/{简称}/）；登记于平台库 sys_module，见[《项目规划说明》](../规划/项目规划说明.md)「平台扩展接入流程」。

| 简称 | 全称/来源 | 项目内含义 | 表前缀 / 业务码 / 事件域 |
| --- | --- | --- | --- |
| sys | system | 系统域（平台库，含权限体系：用户/岗位/角色/菜单/表单/字段/按钮/业务/动作/数据范围等） | sys_ / sys / sys.* |
| wf | workflow | 工作流（流程定义/实例/待办/审批） | wf_ / wf / wf.* |
| rpt | report | 报表（数据集/报表/大屏） | rpt_ / rpt / rpt.* |
| ai | artificial intelligence | AI 域（LLM 适配/RAG/OCR/问答；阶段十五） | ai_ / ai / ai.* |
| pur | purchase | biz·采购模块（申请/订单审批，产品承载） | pur_ / pur / pur.* |
| pay | payment | biz·收付款模块（收款/付款/退款/流水/渠道） | pay_ / pay / pay.* |
| sup | supplier | biz·供应商档案 | sup_ / sup / sup.* |
| sale | sales | biz·销售模块（客户/合同/订单/发货/退货） | sale_ / sale / sale.* |
| wh | warehouse | biz·仓储模块（仓库/库位/库存/出入库/调拨盘点） | wh_ / wh / wh.* |
| user | user | 用户管理（含账号锁定/解锁） | — / user / user.* |
| post | post | 岗位管理（主体链中间实体） | — / post / post.* |
| dept | department | 部门管理（组织架构树、数据权限依据） | — / dept / dept.* |
| role | role | 角色管理与主体绑定 | — / role / role.* |
| dict | dictionary | 字典管理（sys_dict_type / sys_dict_item） | — / dict / dict.* |
| config | configuration | 系统参数（表 sys_config，键字段用 config_key 避 MySQL 保留字） | — / config / config.* |
| dashboard | dashboard | 首页工作台（卡片注册表与模板维护） | — / dashboard / dashboard.* |
| formdesign | form design | 表单定制（布局/字段属性/租户自建字段） | — / formdesign / formdesign.* |
| file | file | 文件管理（上传/下载/分片/预览） | — / file / file.* |
| session | session | 会话管理（在线会话/强制踢出） | — / session / session.* |
| mail | mail | 邮件与短信通知（模板/发送/记录） | — / mail / mail.* |
| task | task | 任务调度（sys_task 入库、Celery 执行） | — / task / task.* |
| archive | archive | 归档管理（两段式归档；归档库 bms_archive） | — / archive / archive.* |
| sso | Single Sign-On | 身份认证（SSO）与 SSO 身份绑定业务 | — / sso / sso.* |
| open | open | 开放接口与 Webhook 管理（/api/open 前缀） | — / open / open.* |
| idp | Identity Provider | 外部 IdP 配置业务（sys_identity_provider） | — / idp / idp.* |
| data | data | 敏感数据脱敏业务（data:plain 明文查看） | — / data / data.* |
| notice | notice | 公告管理（全员公告，sys_notice） | — / notice / notice.* |
| help | help | 帮助中心（分类/文章维护） | — / help / help.* |
| notification | notification | 通知中心（个人站内信/待办提醒） | — / notification / notification.* |
| monitor | monitor | 系统监控（指标/日志/缓存/告警） | — / monitor / monitor.* |
| log | log | 审计日志（操作/登录/数据变更/开放接口，审计管理员） | — / log / log.* |
| recycle | recycle | 回收站（软删除查看/恢复） | — / recycle / recycle.* |
| backup | backup | 备份管理（任务/记录/恢复演练） | — / backup / backup.* |
| import | import | Excel 批量导入 | — / import / import.* |
| export | export | 列表导出 | — / export / export.* |
| tenant | tenant | 租户管理（开通/停用/域名/配额，平台超管） | — / tenant / tenant.* |
| menu | menu | 菜单/表单/按钮/字段维护（平台统一） | — / menu / menu.* |
| business | business | 业务权限维护（sys_business，平台统一） | — / business / business.* |
| action | action | 动作权限维护（sys_action，平台统一） | — / action / action.* |
| i18n | internationalization（国际化） | 国际化管理；_i18n 附表后缀；i18n key 点分命名 | — / i18n / i18n.* |

## 5. 动作码简称 <a id="action"></a>

动作码（sys_action）为按钮级权限的动词缩写，命名 `业务:动作`（如 user:create）：

| 动作码 | 含义 | 典型权限码 |
| --- | --- | --- |
| query | 查询/查看 | wf:query、monitor:view |
| create | 新建 | user:create |
| update | 修改 | role:update |
| delete | 删除 | file:delete |
| manage | 管理（含查询与维护） | dict:manage、task:manage |
| grant | 授权/分配 | role:grant、user:assign_role |
| kick | 强制踢出 | session:kick |
| unlock | 账号解锁 | user:unlock |
| cache | 缓存清理与热数据刷新 | monitor:cache |
| quota | 租户配额配置 | tenant:quota |
| approve | 审批（同意/驳回/撤回） | wf:approve |
| refund | 退款（原收款退回/原付款追回） | payment:refund |
| define | 流程定义与版本管理 | wf:define |
| upload | 上传 | file:upload |
| download | 下载 | file:download |
| call | 以第三方身份调用（scope 授权） | open:call |
| view | 查看（报表/大屏/监控） | rpt:view |
| design | 设计（报表/大屏） | rpt:design |
| execute | 执行 | import:execute |
| send | 发送 | mail:send |
| trigger | 手动触发 | task:trigger |
| reset_pwd | 重置密码 | user:reset_pwd |
| bind | SSO 身份绑定查看/管理 | sso:bind |
| restore | 回收站恢复 | recycle:restore |
| chat | AI 对话 | ai:chat |
| plain | 敏感字段明文查看 | data:plain |

## 6. 通用技术与协议缩写 <a id="tech"></a>

| 缩写 | 全称 | 项目内含义/用法 |
| --- | --- | --- |
| API | Application Programming Interface | 接口；内部 /api/v1、开放 /api/open |
| ORM | Object-Relational Mapping | 对象关系映射（SQLAlchemy 2.0 异步） |
| SQL | Structured Query Language | 结构化查询语言；报表数据集 SQL 建模 |
| DB | Database | 数据库 |
| DDL | Data Definition Language | 数据定义语言；租户自建字段动态 DDL 加列 |
| DML | Data Manipulation Language | 数据操纵语言 |
| JSON | JavaScript Object Notation | 配置/布局存储（layout_config、pref_value 等） |
| TOML | Tom's Obvious Minimal Language | 配置文件格式（config.toml） |
| XML | eXtensible Markup Language | BPMN 流程定义（bpmn_xml） |
| URL | Uniform Resource Locator | 统一资源定位符；预签名 URL 下载 |
| UI | User Interface | 用户界面 |
| ID | Identifier | 标识符；雪花 ID 主键、业务 ID |
| IP | Internet Protocol | 网络地址；IP 白名单、日志记录 |
| CIDR | Classless Inter-Domain Routing | 无类域间路由（IP 白名单可配 CIDR） |
| TTL | Time To Live | 生存时间（Redis key、token 有效期） |
| HTTP / HTTPS | HyperText Transfer Protocol（Secure） | 网络协议；HTTPS 由 nginx TLS 终止 |
| TLS | Transport Layer Security | 传输层安全（证书/终止） |
| CORS | Cross-Origin Resource Sharing | 跨域资源共享（dev 放行，生产同源不开放） |
| CSRF | Cross-Site Request Forgery | 跨站请求伪造（双重提交校验） |
| XSS | Cross-Site Scripting | 跨站脚本攻击（Element Plus 默认转义防护） |
| JWT | JSON Web Token | 双 token：access（30 分钟）+ refresh（14 天滚动） |
| RBAC | Role-Based Access Control | 基于角色的访问控制（主体链 + 权限分层） |
| SSO | Single Sign-On | 单点登录（OIDC 为主 + CAS 兼容） |
| OIDC | OpenID Connect | 基于 OAuth2 的身份认证协议（客户端/Provider） |
| CAS | Central Authentication Service | 中央认证服务（兼容接入） |
| OAuth | Open Authorization | 开放授权（Client Credentials + OIDC 授权码） |
| PBKDF2 | Password-Based Key Derivation Function 2 | 密码哈希算法（标准库实现） |
| SMTP | Simple Mail Transfer Protocol | 邮件发送（aiosmtplib） |
| S3 | Simple Storage Service（协议） | 对象存储协议（MinIO 兼容） |
| BPMN | Business Process Model and Notation | 业务流程建模（SpiffWorkflow 执行、bpmn-js 建模） |
| BI | Business Intelligence | 商业智能（内置报表 BI） |
| E2E | End-to-End | 端到端测试（Playwright） |
| CRUD | Create / Read / Update / Delete | 增删改查（开放范围按 scope 控制） |
| CI | Continuous Integration | 持续集成（GitLab CI 流水线） |
| CD | Continuous Delivery / Deployment | 持续交付/部署 |
| MR | Merge Request | 合并请求（GitLab，main 保护分支） |
| OCR | Optical Character Recognition | 光学字符识别（PaddleOCR，阶段十五） |
| RAG | Retrieval-Augmented Generation | 检索增强生成（Milvus 向量检索，阶段十五） |
| NL2SQL | Natural Language to SQL | 自然语言转 SQL（智能报表助手，阶段十五） |
| ES | ElasticSearch | 全文检索引擎（elasticsearch-py） |
| MQ | Message Queue | 消息队列（本项目指 RocketMQ 事件总线） |
| SPA | Single Page Application | 单页应用（管理端与 H5 均为 SPA） |
| SSR | Server-Side Rendering | 服务端渲染（明确不采用 Nuxt/SSR） |
| H5 | HTML5 | 移动端页面（frontend-mobile，Vue 3 + Vant） |
| PC | Personal Computer | 管理端（frontend，Vue 3 + Element Plus） |
| PITR | Point-in-Time Recovery | 时间点恢复（Binlog/WAL） |
| RTO | Recovery Time Objective | 恢复时间目标（≤ 4 小时） |
| RPO | Recovery Point Objective | 恢复点目标（≤ 1 小时） |
| P99 | 99th percentile | 99 分位延迟（压测目标 P99 ≤ 1s） |
| K8s | Kubernetes | 容器编排（MVP 用 Docker Compose，演进路线迁移） |
| REST | Representational State Transfer | RESTful 资源式接口 |
| CSP | Content Security Policy | 内容安全策略（nginx 响应头） |
| HSTS | HTTP Strict Transport Security | HTTP 严格传输安全（dev 关闭） |
| Webhook | Web hook | 事件推送回调（HMAC 签名、重试） |
| Socket.IO | — | 实时推送协议（Redis 适配器跨实例广播） |
| ACK | Acknowledgement | 确认（Celery ack 确认） |
| AD | Active Directory | 微软目录服务（OIDC/CAS 对接的常见 IdP） |
| LDAP | Lightweight Directory Access Protocol | 轻量目录访问协议（对接场景经 OIDC/CAS 适配） |
| 2FA / MFA | Two-Factor / Multi-Factor Authentication | 双因素/多因素认证（MVP 明确不采用） |

## 7. 组件与产品名称对照 <a id="component"></a>

技术组件与产品一律使用官方英文名（大小写按官方写法），中文文档首现时附中文说明；以下为常用对照：

| 名称 | 中文/说明 | 在项目中的角色 |
| --- | --- | --- |
| FastAPI | Python Web 框架 | 后端 Web 框架 |
| SQLAlchemy | Python ORM 库 | 异步 ORM/Core，多数据源 |
| Alembic | 数据库迁移工具 | schema 版本管理 |
| Pydantic | 数据校验库 | 请求/响应模型（v2） |
| uvicorn | ASGI 服务器 | 后端运行（多 worker） |
| SQLite / MySQL / PostgreSQL / 达梦 DM8 | 四类数据库 | 开发测试用 SQLite；生产三库并行（DM8 信创选配） |
| aiomysql / PyMySQL / psycopg / dmPython | 数据库驱动 | MySQL 异步驱动/同步备用、PostgreSQL 驱动、达梦驱动 |
| Redis | 内存键值存储 | 缓存/会话/限流/任务 broker |
| redis-py | Redis 客户端 | 官方客户端（redis.asyncio） |
| RocketMQ | 消息队列（Apache） | 事件总线（操作日志/Webhook/审计链/模块解耦） |
| Celery | 分布式任务队列 | 定时任务（broker 用 Redis） |
| MinIO | 对象存储 | S3 兼容文件存储 |
| ElasticSearch | 检索引擎 | 全文检索（8.x） |
| Milvus | 向量数据库 | 语义检索（阶段十五，依赖 etcd + MinIO） |
| PaddleOCR | OCR 引擎（百度） | 本地 OCR（阶段十五） |
| PyJWT | JWT 库 | token 签发校验 |
| authlib | OAuth 库 | OIDC 客户端/Provider + CAS |
| Pillow | 图像处理库 | 图形验证码生成 |
| openpyxl | Excel 处理库 | 导入导出 |
| SpiffWorkflow | BPMN 引擎（Python） | 流程执行（线程池） |
| Jinja2 | 模板引擎 | 邮件/通知模板渲染 |
| structlog | 结构化日志库 | JSON 结构化日志 |
| slowapi | 限流库 | 接口级限流（Redis 后端） |
| aiosmtplib | 异步 SMTP 库 | 邮件发送 |
| Babel | 国际化工具 | 开发期文案提取 |
| yitter/IdGenerator | 雪花算法实现 | 全局唯一 ID 生成 |
| python-socketio / socket.io-client | Socket.IO 实现 | 实时推送（后端/前端） |
| OpenTelemetry（otel） | 可观测性框架 | 链路追踪（otel-collector + Jaeger） |
| Jaeger | 链路追踪系统 | trace 存储展示 |
| Vue 3 / Vite / TypeScript | 前端框架/构建/语言 | 双工程（frontend / frontend-mobile） |
| Element Plus / Vant | UI 组件库 | PC 管理端 / 移动端 H5 |
| Vue Router / Pinia | 路由/状态管理 | 前端官方方案 |
| Axios | HTTP 客户端 | 请求拦截/401 刷新 |
| vue-i18n | 国际化库 | 运行时语言包 |
| ECharts | 图表库 | 报表与大屏渲染 |
| bpmn-js | BPMN 建模组件 | 流程拖拽建模 |
| gridstack.js | 网格拖拽库 | 工作台/报表设计器网格布局 |
| vue-flow | 流程图库 | 大屏自由画布 |
| vuedraggable | 拖拽库（SortableJS 封装） | 表单设计器字段排序 |
| ESLint / Prettier | 代码规范工具 | 前端 lint/格式化 |
| Vitest | 前端测试框架 | 组件/单元测试 |
| Playwright | E2E 测试框架 | 端到端测试 |
| Docker / Docker Compose | 容器/编排 | 开发与生产部署一致 |
| nginx | Web 服务器 | 静态托管/反代/TLS/租户路由 |
| Prometheus / Loki / Grafana / Alertmanager | 监控体系 | 指标/日志聚合/展示/告警 |
| GitLab CE / gitlab-runner | 代码托管与 CI | 仓库 + MR + CI + Registry 一体 |
| Renovate | 依赖升级机器人 | 自动提 MR 升级依赖 |
| Kubernetes | 容器编排平台 | 演进路线（当前不采用） |
| locust | 压测工具 | 压测脚本（500 并发 / P99 ≤ 1s） |
| mermaid | 图表渲染库 | 文档图形（HTML 文档内） |
| Keycloak / Authing | 身份平台 | 外部 IdP 示例（OIDC） |
| 企业微信（wecom）/ 钉钉（dingtalk） | 办公平台 | 免登/通知渠道 |
| DeepSeek / Qwen | 大模型 API | LLM 适配层默认外部模型（阶段十五） |
| Ollama / vLLM | 私有化模型服务 | 私有化端点可选（阶段十五） |

## 8. 命名约定后缀与前缀 <a id="suffix"></a>

散落于数据库与基础设施命名中的固定后缀/前缀（写法规则见[《命名规范》](命名规范.md)第 7、10 节，此处统一含义）：

| 片段 | 含义 | 示例 |
| --- | --- | --- |
| {简称}_ | 表前缀：平台域固定（sys_/wf_/rpt_/ai_），业务模块注册制（pur_/pay_/sup_/sale_/wh_），登记于 sys_module | sys_user、pur_purchase_apply、pay_receipt |
| _i18n | 多语言附表（{主表}_i18n，联合主键 主表ID+locale） | sys_menu_i18n、sys_module_i18n |
| _log | 日志/记录表 | sys_operation_log、sys_login_log、sys_task_log、sys_open_log、sys_mail_log、ai_chat_log、wh_stock_log |
| _flow | 流水台账表 | pay_payment_flow |
| _item | 单据明细行（1:N） | pur_purchase_item、sale_order_item |
| _assign | 关联/绑定表（联合主键） | sys_role_assign、sys_user_post |
| _template | 模板表 | sys_mail_template、sys_sms_template、sys_dashboard_template |
| _audit_log | 字段级变更审计表 | sys_data_audit_log |
| id | 主键（雪花 ID，统一命名 id） | sys_user.id |
| {表}_id | 逻辑外键（目标表名_id） | dept_id、role_id、apply_id |
| xxx_at | 时间字段（UTC 存储） | created_at、last_login_at、expire_at |
| is_ | 布尔字段前缀 | is_enabled、is_read、is_top |
| idx_ / uq_ | 普通索引 / 唯一索引前缀 | idx_dept_id、uq_username |
| ext_ | 租户自建字段物理列前缀（动态 DDL） | ext_custom_text |
| BMS_ | 环境变量前缀（UPPER_SNAKE） | BMS_ENV、BMS_JWT_SECRET |
| bms_ | 库名/镜像名/bucket 前缀（小写） | bms_platform、bms-backend、bms-files |
| bms: | Redis key 前缀（冒号分段） | bms:global:captcha:{uuid} |
| {yyyyMM} | 按月分片表后缀（逻辑表名不带） | sys_operation_log_202608 |
| .part | 分片上传临时对象后缀（MinIO，孤儿回收） | file.bin.part |
| 1xxxx-9xxxx | 错误码段（5 位，1-9 段） | 20001（认证） |
| 10xxxx 起 | 业务模块错误码段（6 位，段号两位，注册制） | 100001（采购段） |

> `BMS_`/`bms_`/`bms:` 为本平台默认标识前缀（见《[命名规范](命名规范.md)》第 3 节，源本平台 BMS）；复用本平台的产品沿用，独立部署产品可替换自身前缀。

## 9. 使用规则 <a id="usage"></a>

- 阅读与写作一律以本表为理解口径；同一缩写多义时以"项目内含义"列为准，禁止按通用外义解读（如 open 指开放接口业务、data 指脱敏业务）。
- 模块简称、表前缀、业务码、事件域四者保持一一对应（sys_module 注册制，见[《项目规划说明》](../规划/项目规划说明.md)「平台扩展接入流程」），新增模块先在 sys_module 登记再补充本表。
- 新缩写登记流程：新增模块/缩写 → 在对应文档定稿时同步补充本表（第 3-8 节对应分类）；未登记缩写不得在正式文档中使用。
- 技术组件与产品一律用官方英文名（第 7 节对照），中文文档首现时附中文说明；不缩写组件名（如不写"VUE"、不写"ES 库"代替 ElasticSearch 时应先全称后简称）。
- 本表为文档级约定，随文档变更更新；与[《命名规范》](命名规范.md)、[《项目规划说明》](../规划/项目规划说明.md)冲突时以上述两文档为准并回改本表。

## 10. 检查清单 <a id="checklist"></a>

- □ 文档中出现的英文简称均可在本表查得，含义一致
- □ 模块简称/前缀/业务码/事件域与 sys_module 注册一致（pur↔pur_↔purchase↔purchase.*）
- □ 无自造缩写、无大小写混用（缩写全大写、模块简称全小写、组件官方写法）
- □ 组件名首次出现附中文说明
- □ 新增缩写已登记本表

> 规范文档 · 与《命名规范》《项目规划说明》《文档生成规范》配套
