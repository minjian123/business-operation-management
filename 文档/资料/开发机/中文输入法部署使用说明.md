# 中文输入法部署使用说明

> mjpc 开发机 Fcitx5 中文拼音输入法部署与配置实录 · 2026-08-29

[文档首页](../../文档首页.md) › 资料 › 开发机 › 中文输入法部署使用说明　|　[同级参照：防火墙部署使用说明 →](防火墙部署使用说明.md)　[Steamcommunity_302部署使用说明 →](Steamcommunity_302部署使用说明.md)

## 1. 目的与适用范围 <a id="purpose"></a>

记录开发机 **mjpc**（Ubuntu 26.04.1 LTS，GNOME，Wayland）上英文系统默认输入法（IBus + libpinyin）使用体验不佳，改为 **Fcitx5 + 中文拼音（全拼）** 的完整方案、部署步骤、配置项与排障方法，作为日后重装或迁移的参照。

方案经过确认（2026-08-29）：输入法框架选 Fcitx5 + 中文拼音，输入方式用全拼。

## 2. 背景：为何更换 <a id="background"></a>

mjpc 系统环境与原输入法现状：

| 项 | 现状 |
| --- | --- |
| 系统 | Ubuntu 26.04.1 LTS（resolute），GNOME 桌面，Wayland 会话（`XDG_SESSION_TYPE=wayland`） |
| 原输入法 | IBus 1.5.34 + ibus-libpinyin 1.16.5（Ubuntu 自带） |
| 问题 | 智能拼音的候选词、联想与词库体验一般，中文输入不顺手 |
| 目标 | 更换为候选词/联想/云词库更好的 **Fcitx5 + 中文拼音**，保留全拼习惯 |

在 Wayland + GNOME 下，Fcitx5 是国内用户常用且成熟的方案；GNOME 默认只管理 IBus，需额外配置环境变量与自启动才能让 Fcitx5 接管中文输入。

## 3. 技术方案 <a id="scheme"></a>

| 层 | 组件 | 版本 | 作用 |
| --- | --- | --- | --- |
| 输入法框架 | fcitx5 | 5.1.19 | 输入法容器，管理引擎与候选窗 |
| 中文引擎 | fcitx5-chinese-addons | 5.1.12-1 | 提供拼音（pinyin）、双拼（shuangpin）等 |
| 图形配置 | fcitx5-configtool（fcitx5-config-qt） | — | 打开 fcitx5 图形配置界面 |
| 前端 | gtk3 / gtk4 / qt5 / qt6 frontend | — | 让各类型应用的输入框接入 fcitx5 |
| 候选窗显示 | GNOME 扩展 kimpanel（Input Method Panel） | 91（适配 GNOME Shell 50） | 让 fcitx5 的候选窗在 GNOME Wayland 下显示且**跟随光标** |

部署后本机核实：`/usr/bin/fcitx5`、`/usr/bin/fcitx5-configtool` 均存在；拼音引擎 `/usr/lib/x86_64-linux-gnu/fcitx5/libpinyin.so` 存在。

> **关键技术点**：GNOME 不支持 Wayland 的 input-method 协议，fcitx5 的 Wayland 前端无法让候选窗随光标移动（原生 Wayland 窗口候选窗会固定左上角或不显示）。**kimpanel** 扩展运行在合成器内，能读取相对坐标并在正确位置绘制候选窗，是 GNOME Wayland 下候选窗跟随光标的正解。Electron/Chromium（如 opencode 桌面、VSCode）同样依赖它。

## 4. 部署步骤 <a id="deploy"></a>

以下全部在开发机本地执行，安装类命令需 sudo。下载走 Ubuntu 官方国内镜像（清华 TUNA），已由系统源配置覆盖。

### 4.1 安装 Fcitx5 与中文引擎 <a id="install"></a>

```bash
sudo apt-get update

sudo apt-get install -y --no-install-recommends \
  fcitx5 fcitx5-chinese-addons \
  fcitx5-config-qt \
  fcitx5-frontend-gtk3 fcitx5-frontend-gtk4 \
  fcitx5-frontend-qt5 fcitx5-frontend-qt6 \
  fcitx5-modules
```

### 4.2 切换默认输入法框架 <a id="switch-framework"></a>

Ubuntu 用 im-config（输入法配置工具）管理默认框架。将默认框架切到 fcitx5：

```bash
im-config -n fcitx5
```

执行后用户级配置 `~/.xinputrc` 写入 `run_im fcitx5`，登录时据此启动 fcitx5。可用的框架用 `im-config -l` 查看（本机为 `ibus fcitx5 xim`）。

### 4.3 配置 Wayland 环境变量 <a id="env"></a>

**关键**：Ubuntu 的 im-config 在 Wayland 下其环境变量注入脚本 `/etc/profile.d/im-config_wayland.sh` 被整体禁用（脚本内为 `if false`），GNOME 会话由 `systemd --user` 接管，只读取 `~/.config/environment.d/`。因此必须显式写环境变量文件，否则 GTK/Qt 应用仍会走 IBus 或完全不出输入法。

创建 `~/.config/environment.d/10-fcitx5-env.conf`：

```text
GTK_IM_MODULE=fcitx
QT_IM_MODULE=fcitx
XMODIFIERS=@im=fcitx
SDL_IM_MODULE=fcitx
```

重启登录后可用 `systemctl --user show-environment | grep IM_MODULE` 核对是否已注入（见 7.1）。

### 4.4 配置自启动 <a id="autostart"></a>

GNOME 登录时启动 fcitx5 守护进程。创建 `~/.config/autostart/fcitx5.desktop`：

```text
[Desktop Entry]
Type=Application
Name=Fcitx5
GenericName=Fcitx 5 Input Method
Comment=Input Method
Exec=/usr/bin/fcitx5 -d
Terminal=false
X-GNOME-Autostart-enabled=true
X-GNOME-Autostart-Delay=2
```

`X-GNOME-Autostart-Delay=2` 让 fcitx5 在桌面组件起来后再启动，避免抢在桌面前出错。

### 4.5 设置默认拼音引擎 <a id="default-engine"></a>

让默认输入法组使用拼音而非仅英文键盘。创建 `~/.config/fcitx5/profile`：

```text
[Groups/0]
Name=默认
Default Layout=us
DefaultIM=pinyin

[Groups/0/Items/0]
Name=keyboard-us
Layout=

[Groups/0/Items/1]
Name=pinyin
Layout=

[GroupOrder]
0=默认
```

第一组包含英文键盘（keyboard-us）与拼音（pinyin），默认语言设为拼音，之后在候选窗内按切换键即可中英互切。

### 4.6 安装 kimpanel 输入法面板扩展 <a id="kimpanel"></a>

fcitx5 的 Wayland Diagnose 弹窗会提示 "It is recommended to install Input Method Panel GNOME Shell Extensions"，即推荐安装 GNOME 扩展 **Input Method Panel（`kimpanel@kde.org`）**。没有它，候选窗在 GNOME Wayland 下不跟随光标。

从 GNOME 官方扩展站下载适配当前 GNOME Shell 的版本（本机 GNOME Shell 50.1 对应 v91）：

```bash
# GNOME Shell 50 → version_tag=69345（v91）；其他版本先查 https://extensions.gnome.org/extension/261/kimpanel/
curl -fL -o /tmp/kimpanel.shell-extension.zip \
  "https://extensions.gnome.org/download-extension/kimpanel@kde.org.shell-extension.zip?version_tag=69345"

# 解压到用户扩展目录
mkdir -p ~/.local/share/gnome-shell/extensions/kimpanel@kde.org
unzip -oq /tmp/kimpanel.shell-extension.zip -d ~/.local/share/gnome-shell/extensions/kimpanel@kde.org

# 编译该扩展的 gsettings schema
glib-compile-schemas ~/.local/share/gnome-shell/extensions/kimpanel@kde.org/schemas

# 加入启用名单（保留原有扩展）
gsettings set org.gnome.shell enabled-extensions \
  "['ding@rastersoft.com', 'ubuntu-dock@ubuntu.com', 'tiling-assistant@ubuntu.com', 'kimpanel@kde.org']"
```

核对 metadata 与当前 GNOME Shell 兼容：`python3 -c "import json;print(json.load(open('$HOME/.local/share/gnome-shell/extensions/kimpanel@kde.org/metadata.json'))['shell-version'])"` 应包含当前 Shell 版本号。

> **重要**：GNOME Shell 在启动时缓存了扩展清单，新装扩展必须**重启系统（或注销重登）**才会被 shell 扫描加载。临时用 `gnome-extensions enable kimpanel@kde.org` 在已运行的 shell 上会报"扩展不存在"，属正常——因为 shell 尚未扫描到新目录。

### 4.7 生效 <a id="reload"></a>

配置只在登录会话启动时加载，已运行的进程不会即时切换。保存工作后**重启系统**（比注销更可靠，见 7.4），重新登录后 fcitx5 自动启动、kimpanel 随 GNOME Shell 加载，中文输入与候选窗跟随即对浏览器、编辑器、终端、Electron 应用等生效。

## 5. 配置文件清单 <a id="config-files"></a>

| 路径 | 作用 |
| --- | --- |
| `~/.xinputrc` | im-config 写入：`run_im fcitx5`，登录启动 fcitx5 |
| `~/.config/environment.d/10-fcitx5-env.conf` | Wayland 下注入 GTK/QT/X 输入法环境变量（第 4.3 节） |
| `~/.config/autostart/fcitx5.desktop` | GNOME 登录自启动 fcitx5（第 4.4 节） |
| `~/.config/fcitx5/profile` | 输入法分组与默认引擎：默认拼音（第 4.5 节） |
| `~/.config/fcitx5/config` | 全局配置（快捷键、候选窗等，首次运行由系统生成，可不手改） |
| `~/.local/share/gnome-shell/extensions/kimpanel@kde.org/` | kimpanel 输入法面板扩展文件（第 4.6 节） |

## 6. 使用说明 <a id="usage"></a>

- **中英文切换**：默认快捷键 **Ctrl+Space**（触发激活/停用）。
- **图形配置**：运行 `fcitx5-configtool` 打开配置界面，可修改候选窗、快捷键、切换组等；也可拖拽调整候选词/双拼方案。
- **候选词翻页**：默认 `-` / `=`（或方向键），可在配置界面自定义。
- **输入法组切换**：默认 `Ctrl+Shift` 在组内多个引擎（如键盘 ↔ 拼音）间切换。
- 若系统中同时存在 GNOME 输入源（Super+Space，走 IBus），建议统一使用 Ctrl+Space 在 fcitx5 内切换，避免两套快捷键混淆（见 7.2）。

## 7. 常见问题与故障排查 <a id="troubleshoot"></a>

### 7.1 登录后不出中文输入法 <a id="no-input"></a>

按顺序排查：环境变量是否注入 → fcitx5 是否在跑 → 引擎是否加载。

```bash
# 1) 环境变量是否已注入（应在输出里见到 GTK_IM_MODULE=fcitx 等）
systemctl --user show-environment | grep -iE "IM_MODULE|XMODIFIERS"

# 2) fcitx5 进程
pgrep -a fcitx5

# 3) 拼音引擎
ls /usr/lib/x86_64-linux-gnu/fcitx5/ | grep pinyin
```

- 若 1) 为空：确认 `~/.config/environment.d/10-fcitx5-env.conf` 存在且内容正确，注销重登。
- 若 2) 无进程：确认 `~/.config/autostart/fcitx5.desktop` 存在，或手动 `fcitx5 -d` 试启动。
- 若 3) 无 `libpinyin.so`：补装 `fcitx5-chinese-addons`。
- 若某应用打开前 gsettings 输入源仍是 IBus：注销该应用所属会话再重登生效。

### 7.2 GNOME 输入源与 fcitx5 并存 <a id="gnome-imes"></a>

GNOME 自带"输入源"功能（快捷键 Super+Space）走 IBus，与 fcitx5 的 Ctrl+Space 是两套体系。若同时开启会互相抢键。建议：日常在 fcitx5 内用 Ctrl+Space 处理，GNOME 输入源保持默认即可；gnome-shell 自带的搜索框等界面文本输入仍走 IBus，属 GNOME 层行为，不影响应用内中文输入。

### 7.3 想用双拼 <a id="shuangpin"></a>

`fcitx5-chinese-addons` 已含双拼（shuangpin）引擎。在 `fcitx5-configtool` 的输入法组中新增"双拼"，并在其配置里选方案（小鹤/自然码等）。全拼与双拼可并存，使用组内切换键切换。

### 7.4 候选窗不跟随光标 <a id="candidate-pos"></a>

候选窗固定在屏幕左上角或不显示，是 fcitx5 在 GNOME Wayland 下的典型问题，最可能原因就是未装 kimpanel。按顺序排查：

```bash
# 1) kimpanel 是否被访问 shell 加载（应能查到 kimpanel@kde.org）
gdbus call --session --dest org.gnome.Shell --object-path /org/gnome/Shell \
  --method org.gnome.Shell.Extensions.ListExtensions | grep -o "kimpanel"
# 输出为空说明 shell 未加载它

# 2) 扩展是否在启用名单
gsettings get org.gnome.shell enabled-extensions

# 3) 扩展目录与版本
ls ~/.local/share/gnome-shell/extensions/kimpanel@kde.org/metadata.json
python3 -c "import json;print(json.load(open('$HOME/.local/share/gnome-shell/extensions/kimpanel@kde.org/metadata.json'))['shell-version'])"
```

- 若 1) 为空：说明扩展是运行中的 shell 启动后才放入的，shell 未扫描到。**重启系统**（注销重登对 GNOME Shell 缓存不一定生效）。
- 若 metadata 的 `shell-version` 不含当前 Shell 版本：下载的版本不匹配，回第 4.6 节换对应 `version_tag`。
- 若采用过"让 opencode/Electron 回退 XWayland"（`ELECTRON_OZONE_PLATFORM_HINT=x11`）的旧思路：该变量对 snap 打包的 Electron 应用**无效**（snap 环境不继承宿主自定义变量，opencode 仍以 `ozone-platform=wayland` 启动），且会误导后续排障，应删除。GitHub 仓库中对应的坑见该仓库 `aikiata/fcitx5-gnome-wayland-fix` 的说明（kimpanel 是正解，而非回退 X11）。

## 8. 关联文档 <a id="related"></a>

- 《[防火墙部署使用说明](防火墙部署使用说明.md)》：mjpc 开发机 ufw 防火墙规则口径与启用实录
- 《[Steamcommunity_302部署使用说明](Steamcommunity_302部署使用说明.md)》：mjpc 上的 Steam 社区加速反代 V15 部署与运维
- 《[文档生成规范](../../规范/文档生成规范.md)》：本文档的组织、格式与图形约定
- 《[本地资源](../../用户文档/本地资源.md)》：mjpc 相关机器信息取值（已 gitignore）

> 依《[文档生成规范](../../规范/文档生成规范.md)》编写 · 记录 2026-08-29 mjpc 实际安装、配置与验证结果
